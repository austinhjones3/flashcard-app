import React, { Fragment } from "react";

export default function EditDeck({
  deck,
  setDeck,
  deckUrl,
  deckId,
  error,
  setError,
}) {
  return <Fragment>you do be editing a deck doe</Fragment>;
}
